<div class="ui blue segment">
	<h2><a href="javascript:history.back()"><i class="blue reply icon"></i></a><%= @blog.title %></h2>
	<% if @blog.mpt.present? %>
	  <audio autoplay="autoplay" controls="controls"loop="loop" preload="auto"
	              src="<%= @blog.mpt %>">
	        你的浏览器不支持audio标签
	  </audio>
	<% end %>
	<br/><br/>
	<%= @blog.content.html_safe %>
</div>
